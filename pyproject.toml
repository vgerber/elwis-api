[project]
name = "elwis-api"
version = "0.1.0"
description = ""
readme = "README.md"
dependencies = [
    "pydantic>=2.11.7",
    "zeep>=4.3.1",
]


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
